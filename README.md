# Copyright 2025 MCRcortex

## All rights reserved.

Do not redistribute.

**THE SOFTWARE IS PROVIDED "AS IS", WITHOUT WARRANTY OF ANY KIND, EXPRESS OR IMPLIED, INCLUDING BUT NOT LIMITED TO THE WARRANTIES OF MERCHANTABILITY, FITNESS FOR A PARTICULAR PURPOSE AND NONINFRINGEMENT. IN NO EVENT SHALL THE AUTHORS OR COPYRIGHT HOLDERS BE LIABLE FOR ANY CLAIM, DAMAGES OR OTHER LIABILITY, WHETHER IN AN ACTION OF CONTRACT, TORT OR OTHERWISE, ARISING FROM, OUT OF OR IN CONNECTION WITH THE SOFTWARE OR THE USE OR OTHER DEALINGS IN THE SOFTWARE.**

这只是为**1.20.1**编译的Voxy版本，包含了对整合包中各种模组的兼容性修复、性能优化、稳定性改进以及上游功能移植。

本次移植为AI产物，感谢各位作者的创作，尤其是MCRcortex对Minecraft社区的贡献。

### 主要变更
- **兼容性与稳定性修复**
    - 修复了在 Redis、LMDB 及内存存储后端读取数据时的缓冲区溢出风险，增加了容量检查并能正确抛出异常。
    - 修复了 RocksDB 迭代器未正确关闭的问题（使用 try-with-resources）。
    - 修复了区块数据摄取时的线程安全问题。
    - 修复了映射器中的状态处理和错误处理逻辑。
    - 修复了获取方块颜色时可能出现的空世界参数异常。（针对部分mod方块的修复）。
    - **Ecliptic-Seasons 兼容性**：通过 `ILightingSupplier` 接口实现与节气模组的完全兼容，支持季节光照修改。
- **性能优化**
    - 优化渲染系统：将 `glFinish` 替换为 `glFlush` 并引入 GPU 围栏（Fence）同步，大幅减少 CPU 渲染等待时间，优化内存释放流程。
    - 优化体素化（Voxelization）过程：引入对象池复用，重构光照数据获取方式，减少内存分配开销。
    - **内存对齐与上传限流**：为显存上传添加严格对齐检查，防止 AMD/旧显卡驱动崩溃；恢复几何上传限流逻辑，避免突发掉帧。
- **⚠️ 重大修复：LOD 生成缺失问题**
    - **彻底修复了 `VoxelIngestService` 的任务积压 Bug**：修正了信号量（Semaphore）释放逻辑，解决了之前每 16 个区块任务仅处理 1 个的严重缺陷。
    - **优化 LOD 触发时机**：确保区块首次渲染时即刻触发 LOD 摄取，大幅提升了新区块的 LOD 生成可靠性。
    - **性能权重平衡**：调整了摄取服务的 CPU 优先级，防止其过度占用资源导致渲染生成服务"饿死"。
    - **高速移动支持**：放宽了区块摄取的状态门槛，显著提升了高速移动时的 LOD 同步响应速度。
- **上游功能移植 (voxy-dev)**
    - **区域渲染管理系统**：新增 `RenderRegionManager`、`SectionFadeManager`、`BatchUploadQueue`，实现区块淡入效果和批量上传优化。
    - **软件光栅化器**：新增 `SoftwareRasterizer` 和 `SoftwareModelTextureBakery`，支持 CPU 端模型纹理烘焙作为 GPU 回退方案。
    - **动态大气雾着色**：移植雾效渲染系统，支持基于太阳角度的动态雾色计算和 Mie 散射模拟。
    - **Windows GPU 选择器**：新增 `GPUSelectorWindows2`，支持多显卡环境下的 GPU 选择。
    - **调试与存储工具**：新增 `DebugUtils`、`WorldConfigStorage`、`IStoredSectionPositionIterator` 等辅助工具。
- **稳定性增强**
    - **子线程异常捕获**：添加异步网格构建线程的异常捕获机制，避免静默崩溃导致的假死现象。
    - **AMD 显卡驱动修复**：为 `glDispatchCompute` 添加零值防护，防止 AMD 驱动错误日志泛滥。
- **构建与环境**
    - 稳定了 Fabric Loom 版本，移除特定的 CI 条件逻辑，确保构建的一致性。
    - **Fabric 平台兼容性修复**：修复了在 Fabric 原生环境下由于 Mixin 方法签名不匹配导致的启动崩溃问题（针对 Sodium 0.5.13）。
    - 更新模组版本至 `0.2.11-alpha`。
### 测试
经测试，使用信雅联接后在1.20.1版本的forge端目前较为正常，Fabric 原生端也已修复崩溃问题。Ecliptic-Seasons 兼容性已验证通过。

---

This is a Voxy version compiled specifically for **1.20.1**, featuring compatibility fixes for various mods, performance optimizations, stability improvements, and upstream feature ports.

This port is generated by AI, thanks to the work of all authors, especially MCRcortex's contributions to the Minecraft community.

### Key Changes
- **Compatibility & Stability**
    - Fixed buffer overflow risks in Redis, LMDB, and Memory storage backends by adding capacity checks.
    - Fixed RocksDB iterator leaks using try-with-resources.
    - Resolved thread-safety issues during chunk data ingestion.
    - Improved state and error handling logic in the mapper.
    - Fixed null world parameter exceptions during block color retrieval.
    - **Ecliptic-Seasons Compatibility**: Full compatibility with the seasons mod via `ILightingSupplier` interface, supporting seasonal lighting modifications.
- **Performance Optimizations**
    - Rendering: Replaced `glFinish` with `glFlush` and implemented GPU Fence synchronization to reduce CPU wait time and optimize memory release.
    - Voxelization: Optimized lighting data management with object pooling and refactored data access for better performance.
    - **Memory Alignment & Upload Throttling**: Added strict alignment checks for VRAM uploads to prevent AMD/older GPU driver crashes; restored geometry upload throttling to avoid lag spikes.
- **⚠️ CRITICAL FIX: LOD Generation Issues**
    - **Resolved `VoxelIngestService` Task Backlog**: Fixed a critical semaphore release bug that caused only 1/16 of tasks to be processed per section.
    - **Optimized LOD Triggering**: Ensured LOD ingestion is triggered immediately upon initial render, significantly improving generation reliability.
    - **Balanced Performance Weights**: Adjusted service priorities to prevent the ingest service from starving the render generation service.
    - **High-Speed Movement Support**: Relaxed chunk status requirements for ingestion, greatly enhancing LOD synchronization speed during fast movement.
- **Upstream Feature Ports (voxy-dev)**
    - **Region Rendering System**: Added `RenderRegionManager`, `SectionFadeManager`, `BatchUploadQueue` for chunk fade-in effects and batch upload optimization.
    - **Software Rasterizer**: Added `SoftwareRasterizer` and `SoftwareModelTextureBakery` for CPU-side model texture baking as a GPU fallback.
    - **Dynamic Atmospheric Fog**: Ported fog rendering system with dynamic fog color calculation based on sun angle and Mie scattering simulation.
    - **Windows GPU Selector**: Added `GPUSelectorWindows2` for GPU selection in multi-GPU environments.
    - **Debug & Storage Utilities**: Added `DebugUtils`, `WorldConfigStorage`, `IStoredSectionPositionIterator` and other helper utilities.
- **Stability Enhancements**
    - **Async Thread Exception Handling**: Added exception capture mechanism for async mesh building threads to prevent silent crashes causing deadlocks.
    - **AMD GPU Driver Fix**: Added zero-value guard for `glDispatchCompute` to prevent AMD driver error log flooding.
- **Build & Environment**
    - Stabilized Fabric Loom version and cleaned up build scripts for better reproducibility.
    - **Fabric Compatibility Fix**: Resolved startup crashes in native Fabric environments caused by Mixin method signature mismatches with Sodium 0.5.13.
    - Updated mod version to `0.2.11-alpha`.
